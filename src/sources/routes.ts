const routes = {
    root: "/",
    dashboard: "/dashboard",
    login: "/auth/login",
    register: "/auth/register",
    api: {
        client: {
            login: "/api/auth/login",
        }
    }
}

export default routes;